name: Sync Fork

on:
  schedule:
    - cron: "*/5 * * * *"   # every 5 minutes
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout fork
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Add upstream
        run: |
          git remote add upstream https://github.com/leadpoet/leadpoet.git

      - name: Fetch upstream
        run: git fetch upstream

      - name: Sync main
        run: |
          git checkout main
          git merge upstream/main --no-edit

      - name: Push changes
        run: git push origin main